<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Students can only access their own profile -->
        <record id="rule_student_own_profile" model="ir.rule">
            <field name="name">Student Own Profile</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="domain_force">[('id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('lms_module.group_student'))]"/>
        </record>

        <!-- LMS Managers can see all students -->
        <record id="rule_manager_all_students" model="ir.rule">
            <field name="name">Manager Access All Students</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="domain_force">[('is_student', '=', True)]</field>
            <field name="groups" eval="[(4, ref('lms_module.group_lms_manager'))]"/>
        </record>

        <!-- Admin can see all -->
        <record id="rule_admin_all_access" model="ir.rule">
            <field name="name">Admin Full Access</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_system'))]"/>
        </record>

    </data>
</odoo>
